{
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18"
  },
  "scripts": {
    "reset-db": "npm --prefix backend run reset-db"
  }
}
